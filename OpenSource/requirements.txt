# ── Web framework ──────────────────────────────────────────────────────────────
streamlit==1.32.0

# ── LLM: Ollama (local open-source models) ────────────────────────────────────
# Ollama Python SDK wraps the REST API; requests is used directly for fine control
ollama==0.2.1
requests==2.31.0

# ── Voice: STT (OpenAI Whisper) + TTS (gTTS) ──────────────────────────────────
openai==1.14.0          # Whisper STT — optional, only for voice transcription
gTTS==2.5.1             # Text-to-speech playback
pydub==0.25.1           # Audio format conversion
SpeechRecognition==3.10.1

# ── Database ────────────────────────────────────────────────────────────────────
sqlalchemy==2.0.28
pymysql==1.1.0          # MySQL driver (production); SQLite is built-in

# ── Google Sheets ───────────────────────────────────────────────────────────────
gspread==6.0.2
google-auth==2.28.2
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
google-api-python-client==2.120.0

# ── Email ────────────────────────────────────────────────────────────────────────
sendgrid==6.11.0

# ── Scheduler ────────────────────────────────────────────────────────────────────
apscheduler==3.10.4

# ── Utilities ────────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
pandas==2.2.1
pydantic==2.6.3
aiohttp==3.9.3

# ── Azure (production) ────────────────────────────────────────────────────────────
azure-identity==1.15.0
azure-keyvault-secrets==4.7.0

# ── Testing ────────────────────────────────────────────────────────────────────────
pytest==8.1.0
pytest-asyncio==0.23.5
